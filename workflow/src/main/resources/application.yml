server:
  port: 1201
spring:
  application:
    name: workflow
  data:
    mongodb:
      uri: mongodb://
      host: xxx.xxx.xxx.xxx
      port: 27017
      database: lfs
  redis:
    port: 6379
    host: xxx.xxx.xxx.xxx
    password: xxxxxx
    jedis:
      pool:
        max-idle: 500
        min-idle: 50
        max-active: 1000
        max-wait: 2000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka


